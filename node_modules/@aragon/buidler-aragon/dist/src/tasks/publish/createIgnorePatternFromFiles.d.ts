/**
 * Reads ignore files from disk and aggregates their glob patterns
 * into a single array
 * @param rootPath Dir to find ignore files
 */
export default function createIgnorePatternFromFiles(rootPath: string): string[];
//# sourceMappingURL=createIgnorePatternFromFiles.d.ts.map